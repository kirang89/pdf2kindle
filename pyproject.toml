[project]
name = "pdf2kindle"
version = "0.1.0"
description = "Convert PDFs to Kindle-optimized EPUBs"
requires-python = ">=3.11"
dependencies = [
    "epubcheck>=0.5.1",
]

[tool.uv]
dev-dependencies = []
